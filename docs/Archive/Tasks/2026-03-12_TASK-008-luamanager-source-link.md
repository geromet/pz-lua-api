> **COMPLETED 2026-03-12** — Added "LuaManager" button to `#globals-header` in `index.html`. Wired in `initGlobals()` via `onclick` to call `showSourceByPath('zombie/Lua/LuaManager.java')`.

# TASK-008: Add LuaManager source link to Globals tab header
**Resolves:** BUG-005
